# neopub

## Usage

To run a local server, `cd servers/node && yarn start`.
To point the client at that, `echo "REACT_APP_HOST_PREFIX=http://localhost:8888" > frontend/.env.local && cd frontend && yarn start`.
